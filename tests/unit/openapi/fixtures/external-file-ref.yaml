openapi: 3.1.0
info:
  title: External file
  version: 1.0.0
servers:
  - url: https://api.example.com
paths:
  /a:
    get:
      operationId: a
      responses:
        '200':
          description: ok
          content:
            application/json:
              schema:
                $ref: './common.yaml#/Thing'
