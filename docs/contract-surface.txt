# Semantic surface of src/core/public-types.ts
# Generated by scripts/contract-surface.mjs — do not edit by hand.
# 69 exported symbols.

interface AdapterDescriptor {
    readonly capabilities: ReadonlyArray<string>;
    readonly implementationVersion: string;
    readonly kind: "protocol" | "payment" | "storage";
    readonly name: string;
    readonly status: "stable" | "experimental" | "planned";
    readonly supportedSpec: string;
    readonly unsupported?: ReadonlyArray<string>;
  }

interface AdapterHealth {
    readonly checkedAt: string;
    readonly detail?: string;
    readonly durationMs?: number;
    readonly status: "pass" | "warn" | "fail";
  }

interface BackendExecutor {
    call: (handler: BackendHandler, request: BackendRequest) => Promise<BackendResponse>;
  }

interface BackendHandler {
    readonly headers?: Readonly<Record<string, string>>;
    readonly method: BackendMethod;
    readonly timeoutMs?: number;
    readonly type: "http";
    readonly url: string;
  }

interface BackendRequest {
    readonly input: unknown;
    readonly requestId: string;
    readonly resourceId: string;
  }

interface BackendResponse {
    readonly body: unknown;
    readonly durationMs: number;
    readonly headers: Readonly<Record<string, string>>;
    readonly status: number;
  }

interface CanonicalRequest {
    readonly input: unknown;
    readonly metadata?: Readonly<Record<string, unknown>>;
    readonly payment?: PaymentSubmission;
    readonly protocol: ProtocolName;
    readonly receivedAt: string;
    readonly requestId: string;
    readonly resourceId: string;
  }

interface Clock {
    monotonicMs: () => number;
    now: () => Date;
    nowIso: () => IsoTimestamp;
  }

interface CommerceErrorInfo {
    readonly code: "CONFIG_INVALID" | "RESOURCE_NOT_FOUND" | "INPUT_INVALID" | "PAYMENT_REQUIRED" | "PAYMENT_INVALID" | "PAYMENT_REPLAYED" | "PAYMENT_PROVIDER_UNAVAILABLE" | "PAYMENT_SETTLEMENT_FAILED" | "BACKEND_TIMEOUT" | "BACKEND_ERROR" | "PROTOCOL_UNSUPPORTED" | "GATEWAY_BUSY" | "STORAGE_ERROR" | "INTERNAL_ERROR";
    readonly details?: Readonly<Record<string, unknown>>;
    readonly httpStatus: number;
    readonly message: string;
    readonly requestId?: string;
    readonly resourceId?: string;
    readonly retryable: boolean;
  }

interface CommerceErrorOptions {
    readonly cause?: unknown;
    readonly details?: Readonly<Record<string, unknown>>;
    readonly httpStatus?: number;
    readonly requestId?: string;
    readonly resourceId?: string;
  }

interface CommerceEvent {
    readonly adapter?: string;
    readonly at: string;
    readonly data?: Readonly<Record<string, unknown>>;
    readonly durationMs?: number;
    readonly id: string;
    readonly paymentProvider?: string;
    readonly requestId: string;
    readonly resourceId?: string;
    readonly status?: "ok" | "error";
    readonly type: "resource.discovered" | "resource.requested" | "payment.required" | "payment.rejected" | "payment.verified" | "payment.settled" | "backend.called" | "backend.failed" | "resource.delivered";
  }

interface CommerceReceipt {
    readonly backendStatus: number;
    readonly deliveredAt: string;
    readonly durationMs?: number;
    readonly id: string;
    readonly metadata?: Readonly<Record<string, unknown>>;
    readonly payment?: PaymentResult;
    readonly protocol?: string;
    readonly requestId: string;
    readonly resourceId: string;
  }

interface CommerceResource {
    readonly description?: string;
    readonly exposedVia: ReadonlyArray<ProtocolName>;
    readonly handler: BackendHandler;
    readonly id: string;
    readonly inputSchema?: JsonSchema;
    readonly name: string;
    readonly outputSchema?: JsonSchema;
    readonly paymentMethods: ReadonlyArray<"x402">;
    readonly pricing: Pricing;
  }

interface DeliveredOutcome {
    readonly backendStatus: number;
    readonly body: unknown;
    readonly durationMs: number;
    readonly headers?: Readonly<Record<string, string>>;
    readonly kind: "delivered";
    readonly payment?: PaymentResult;
    readonly receipt: CommerceReceipt;
    readonly requestId: string;
    readonly resourceId: string;
  }

interface DeliverySummary {
    readonly deliveredAt: string;
    readonly payment?: { readonly status: "verified" | "settled" | "rejected"; readonly amount: DecimalAmount; readonly currency: string; readonly externalReference?: string; readonly network?: string; };
    readonly receiptId: string;
    readonly requestId: string;
    readonly resourceId: string;
  }

interface ErrorEnvelope {
    readonly code: "CONFIG_INVALID" | "RESOURCE_NOT_FOUND" | "INPUT_INVALID" | "PAYMENT_REQUIRED" | "PAYMENT_INVALID" | "PAYMENT_REPLAYED" | "PAYMENT_PROVIDER_UNAVAILABLE" | "PAYMENT_SETTLEMENT_FAILED" | "BACKEND_TIMEOUT" | "BACKEND_ERROR" | "PROTOCOL_UNSUPPORTED" | "GATEWAY_BUSY" | "STORAGE_ERROR" | "INTERNAL_ERROR";
    readonly details?: Readonly<Record<string, unknown>>;
    readonly message: string;
    readonly requestId?: string;
    readonly resourceId?: string;
    readonly retryable: boolean;
    readonly status: "error";
  }

interface EventSink {
    emit: (event: CommerceEvent) => Promise<void>;
  }

interface ExecutionPipeline {
    execute: (request: CanonicalRequest) => Promise<ExecutionOutcome>;
  }

interface HttpProtocolAdapter {
    handleHttp: (req: IncomingMessage, res: ServerResponse) => Promise<void>;
    health: () => Promise<AdapterHealth>;
    readonly descriptor: AdapterDescriptor;
    readonly mountPath: string;
    readonly name: ProtocolName;
    start: (context: ProtocolAdapterContext) => Promise<void>;
    stop: () => Promise<void>;
  }

interface IdGenerator {
    next: (prefix?: string) => string;
  }

interface ListOptions {
    readonly limit?: number;
    readonly requestId?: string;
  }

interface Logger {
    child: (bindings: Record<string, unknown>) => Logger;
    debug: (obj: Record<string, unknown>, msg?: string) => void;
    error: (obj: Record<string, unknown>, msg?: string) => void;
    info: (obj: Record<string, unknown>, msg?: string) => void;
    warn: (obj: Record<string, unknown>, msg?: string) => void;
  }

interface PaymentAttempt {
    readonly amount: string;
    readonly createdAt: string;
    readonly currency: string;
    readonly externalReference?: string;
    readonly id: string;
    readonly payee?: string;
    readonly payer?: string;
    readonly provider: string;
    readonly rejectionReason?: string;
    readonly replayKey: string;
    readonly requestId: string;
    readonly resourceId: string;
    readonly status: "verified" | "settled" | "rejected" | "reserved" | "failed" | "settlement-uncertain";
    readonly updatedAt: string;
  }

interface PaymentAttemptReservation {
    readonly amount: string;
    readonly currency: string;
    readonly payee?: string;
    readonly payer?: string;
    readonly provider: string;
    readonly replayKey: string;
    readonly requestId: string;
    readonly resourceId: string;
  }

interface PaymentAttemptUpdate {
    readonly externalReference?: string;
    readonly rejectionReason?: string;
    readonly replayKey: string;
    readonly status: "verified" | "settled" | "rejected" | "reserved" | "failed" | "settlement-uncertain";
  }

interface PaymentChallenge {
    readonly accepts: ReadonlyArray<Readonly<Record<string, unknown>>>;
    readonly envelope?: Readonly<Record<string, unknown>>;
    readonly provider: "x402";
    readonly version: string;
  }

interface PaymentContext {
    readonly amount: string;
    readonly currency: string;
    readonly metadata?: Readonly<Record<string, unknown>>;
    readonly requestId: string;
    readonly requestedAt: string;
    readonly resource: CommerceResource;
  }

interface PaymentProvider {
    createRequirement: (context: PaymentContext) => Promise<PaymentRequirement>;
    health: () => Promise<AdapterHealth>;
    readonly descriptor: AdapterDescriptor;
    readonly name: "x402";
    settle: (context: PaymentSettlementContext) => Promise<PaymentResult>;
    verify: (context: PaymentVerificationContext) => Promise<PaymentResult>;
  }

interface PaymentRequiredEnvelope {
    readonly code: "PAYMENT_REQUIRED";
    readonly message: string;
    readonly payment: { readonly provider: PaymentMethodName; readonly version: string; readonly amount: DecimalAmount; readonly currency: string; readonly destination: string; readonly network?: string; readonly asset?: string; readonly expiresAt?: IsoTimestamp; readonly accepts: readonly Readonly<Record<string, unknown>>[]; readonly envelope?: Readonly<Record<string, unknown>>; };
    readonly requestId: string;
    readonly resourceId: string;
    readonly status: "payment-required";
  }

interface PaymentRequiredOutcome {
    readonly kind: "payment-required";
    readonly requestId: string;
    readonly requirement: PaymentRequirement;
    readonly resourceId: string;
  }

interface PaymentRequirement {
    readonly amount: string;
    readonly asset?: string;
    readonly challenge: PaymentChallenge;
    readonly currency: string;
    readonly destination: string;
    readonly expiresAt?: string;
    readonly id: string;
    readonly metadata?: Readonly<Record<string, unknown>>;
    readonly network?: string;
    readonly provider: "x402";
    readonly requestId: string;
    readonly resourceId: string;
  }

interface PaymentResult {
    readonly amount: string;
    readonly asset?: string;
    readonly currency: string;
    readonly externalReference?: string;
    readonly metadata?: Readonly<Record<string, unknown>>;
    readonly network?: string;
    readonly payee?: string;
    readonly payer?: string;
    readonly provider: "x402";
    readonly rejectionReason?: string;
    readonly replayKey?: string;
    readonly settledAt?: string;
    readonly status: "verified" | "settled" | "rejected";
  }

interface PaymentSettlementContext {
    readonly requestId: string;
    readonly requirement: PaymentRequirement;
    readonly resource: CommerceResource;
    readonly submission: PaymentSubmission;
    readonly verification: PaymentResult;
  }

interface PaymentSubmission {
    readonly method: "x402";
    readonly payload: string;
  }

interface PaymentVerificationContext {
    readonly requestId: string;
    readonly requirement: PaymentRequirement;
    readonly resource: CommerceResource;
    readonly submission: PaymentSubmission;
  }

interface ProtocolAdapter {
    health: () => Promise<AdapterHealth>;
    readonly descriptor: AdapterDescriptor;
    readonly name: ProtocolName;
    start: (context: ProtocolAdapterContext) => Promise<void>;
    stop: () => Promise<void>;
  }

interface ProtocolAdapterContext {
    readonly clock: Clock;
    readonly events: EventSink;
    readonly ids: IdGenerator;
    readonly logger: Logger;
    readonly pipeline: ExecutionPipeline;
    readonly publicBaseUrl: string;
    readonly resources: ResourceRegistry;
  }

interface ReceiptStore {
    appendEvent: (event: CommerceEvent) => Promise<void>;
    close: () => Promise<void>;
    countReceipts: () => Promise<number>;
    countUndeliveredReceipts: () => Promise<number>;
    getReceipt: (id: string) => Promise<CommerceReceipt | undefined>;
    health: () => Promise<AdapterHealth>;
    init: () => Promise<void>;
    listEvents: (options?: ListOptions) => Promise<readonly CommerceEvent[]>;
    listPaymentAttempts: (options?: ListOptions) => Promise<readonly PaymentAttempt[]>;
    listReceipts: (options?: ListOptions) => Promise<readonly CommerceReceipt[]>;
    readonly descriptor: AdapterDescriptor;
    reservePaymentAttempt: (reservation: PaymentAttemptReservation) => Promise<PaymentAttempt>;
    saveReceipt: (receipt: CommerceReceipt) => Promise<void>;
    updatePaymentAttempt: (update: PaymentAttemptUpdate) => Promise<void>;
  }

interface ResourceRegistry {
    get: (id: string) => CommerceResource | undefined;
    has: (id: string) => boolean;
    list: () => readonly CommerceResource[];
    listExposedVia: (protocol: ProtocolName) => readonly CommerceResource[];
  }

type BackendMethod = BackendMethod

type CommerceErrorCode = "CONFIG_INVALID" | "RESOURCE_NOT_FOUND" | "INPUT_INVALID" | "PAYMENT_REQUIRED" | "PAYMENT_INVALID" | "PAYMENT_REPLAYED" | "PAYMENT_PROVIDER_UNAVAILABLE" | "PAYMENT_SETTLEMENT_FAILED" | "BACKEND_TIMEOUT" | "BACKEND_ERROR" | "PROTOCOL_UNSUPPORTED" | "GATEWAY_BUSY" | "STORAGE_ERROR" | "INTERNAL_ERROR"

type CommerceEventType = "resource.discovered" | "resource.requested" | "payment.required" | "payment.rejected" | "payment.verified" | "payment.settled" | "backend.called" | "backend.failed" | "resource.delivered"

type DecimalAmount = string

type ExecutionOutcome = ExecutionOutcome

type IsoTimestamp = string

type JsonSchema = JsonSchema

type PaymentMethodName = "x402"

type Pricing = Pricing

type ProtocolName = ProtocolName

value COMMERCE_ERROR_CODES: readonly ["CONFIG_INVALID", "RESOURCE_NOT_FOUND", "INPUT_INVALID", "PAYMENT_REQUIRED", "PAYMENT_INVALID", "PAYMENT_REPLAYED", "PAYMENT_PROVIDER_UNAVAILABLE", "PAYMENT_SETTLEMENT_FAILED", "BACKEND_TIMEOUT", "BACKEND_ERROR", "PROTOCOL_UNSUPPORTED", "GATEWAY_BUSY", "STORAGE_ERROR", "INTERNAL_ERROR"]

value COMMERCE_ERROR_HTTP_STATUS: Readonly<Record<"CONFIG_INVALID" | "RESOURCE_NOT_FOUND" | "INPUT_INVALID" | "PAYMENT_REQUIRED" | "PAYMENT_INVALID" | "PAYMENT_REPLAYED" | "PAYMENT_PROVIDER_UNAVAILABLE" | "PAYMENT_SETTLEMENT_FAILED" | "BACKEND_TIMEOUT" | "BACKEND_ERROR" | "PROTOCOL_UNSUPPORTED" | "GATEWAY_BUSY" | "STORAGE_ERROR" | "INTERNAL_ERROR", number>>

value COMMERCE_EVENT_TYPES: readonly ["resource.discovered", "resource.requested", "payment.required", "payment.rejected", "payment.verified", "payment.settled", "backend.called", "backend.failed", "resource.delivered"]

value CommerceError: typeof CommerceError

value DEFAULT_BACKEND_TIMEOUT_MS: 10000

value DELIVERY_SUMMARY_META_KEY: "agent-commerce/delivery"

value NOOP_LOGGER: Logger

value PAYMENT_HEADER: "payment-signature"

value PAYMENT_INPUT_FIELD: "_payment"

value PAYMENT_REQUIRED_HEADER: "payment-required"

value PAYMENT_RESPONSE_HEADER: "payment-response"

value RETRYABLE_ERROR_CODES: ReadonlySet<"CONFIG_INVALID" | "RESOURCE_NOT_FOUND" | "INPUT_INVALID" | "PAYMENT_REQUIRED" | "PAYMENT_INVALID" | "PAYMENT_REPLAYED" | "PAYMENT_PROVIDER_UNAVAILABLE" | "PAYMENT_SETTLEMENT_FAILED" | "BACKEND_TIMEOUT" | "BACKEND_ERROR" | "PROTOCOL_UNSUPPORTED" | "GATEWAY_BUSY" | "STORAGE_ERROR" | "INTERNAL_ERROR">

value isCommerceError: (value: unknown) => value is CommerceError

value isHttpProtocolAdapter: (adapter: ProtocolAdapter) => adapter is HttpProtocolAdapter

value isPaymentRequiredEnvelope: (value: unknown) => value is PaymentRequiredEnvelope

value systemClock: Clock

value toCommerceError: (value: unknown, fallbackCode?: CommerceErrorCode, fallbackMessage?: string) => CommerceError

value toDeliverySummary: (outcome: DeliveredOutcome) => DeliverySummary

value toErrorEnvelope: (error: CommerceError) => ErrorEnvelope

value toPaymentRequiredEnvelope: (outcome: PaymentRequiredOutcome) => PaymentRequiredEnvelope
